{
  "name": "hosonan",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "site/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm test --workspaces"
  }
}
